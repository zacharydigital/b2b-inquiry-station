<button
  id="quote-cart-fab"
  class="hidden fixed right-6 bottom-24 z-30 bg-accent text-white w-14 h-14 rounded-[var(--radius-card)] border border-accent shadow-[var(--shadow-elevated)] flex items-center justify-center hover:bg-accent-hover transition-colors"
  aria-label="Open quote cart"
>
  <span class="i-carbon-shopping-cart text-xl" aria-hidden="true"></span>
  <span
    id="cart-badge"
    class="absolute -top-1 -right-1 bg-gray-900 text-white text-xs w-5 h-5 rounded-[var(--radius-input)] flex items-center justify-center hidden"
  >0</span>
</button>

<script>
  import { isCartOpen, getCartCount } from '../../lib/store';

  // Show button if cart has items
  function updateButton() {
    const btn = document.getElementById('quote-cart-fab');
    const badge = document.getElementById('cart-badge');
    if (!btn || !badge) return;
    const count = getCartCount();
    if (count > 0) {
      btn.classList.remove('hidden');
      btn.classList.add('flex');
      badge.textContent = String(count);
      badge.classList.remove('hidden');
    } else {
      btn.classList.add('hidden');
      btn.classList.remove('flex');
    }
  }

  document.getElementById('quote-cart-fab')?.addEventListener('click', () => {
    isCartOpen.set(true);
  });

  // Poll for updates
  setInterval(updateButton, 1000);
</script>
